{
  "name": "river-crossing-puzzle-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd river-crossing-puzzle && npm start",
    "build": "cd river-crossing-puzzle && npm install && npm run build",
    "test": "cd river-crossing-puzzle && npm test",
    "deploy": "npm run build && npx wrangler deploy --assets=./river-crossing-puzzle/build"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}